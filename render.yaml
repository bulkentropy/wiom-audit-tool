services:
  - type: web
    name: wiom-audit
    env: node
    buildCommand: npm install && node backend/mockDataGen.js
    startCommand: node backend/server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
